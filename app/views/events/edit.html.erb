<div class="bg-light border-bottom py-3 mb-4">
  <div class="container">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb mb-1">
        <li class="breadcrumb-item"><a href="<%= root_path %>">Accueil</a></li>
        <li class="breadcrumb-item"><a href="<%= event_path(@event) %>"><%= @event.title %></a></li>
        <li class="breadcrumb-item active">Modifier</li>
      </ol>
    </nav>
    <h2 class="fw-bold mb-0">✏️ Modifier l'événement</h2>
  </div>
</div>

<div class="container mb-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-body p-4">
          <%= render 'events/form' %>
        </div>
      </div>
    </div>
  </div>
</div>